# 🆓 راهنمای استقرار ۱۰۰٪ رایگان TeleStream (بدون نیاز به کارت بانکی)

پلتفرم Hugging Face به دلیل سوءاستفاده‌های ماینینگ، بخش Docker را نیازمند کارت اعتباری (Credit Card) کرده است. اما شما می‌توانید از **۳ روش کاملاً رایگان** زیر بدون نیاز به هیچ کارت بانکی استفاده کنید:

---

## 🌟 روش اول: هاگینگ‌فیس با SDK رایگان Gradio (پیشنهادی - ۱۶ گیگابایت رم رایگان!)

در Hugging Face، انتخاب **Gradio SDK** کاملاً رایگان است و **هیچ کارت بانکی یا هزینه‌ای نمی‌خواهد**.
ما ساختار پروژه را طوری آماده کرده‌ایم که فایل‌های `app.py` و `packages.txt` به طور خودکار جاوا را روی لینوکس هاگینگ‌فیس نصب کرده و ربات را در پس‌زمینه اجرا می‌کنند.

### مراحل:
1. به آدرس [huggingface.co/new-space](https://huggingface.co/new-space) بروید.
2. نام دلخواه بگذارید (مثلاً `telestream-bot`).
3. در قسمت **Space SDK** گزینه **Gradio** را انتخاب کنید (به هیچ وجه Docker را نزنید).
4. لایسنس را روی `mit` بگذارید و روی **Create Space** کلیک کنید.
5. به تب **Settings** بروید و در بخش **Variables and secrets** موارد زیر را با دکمه **New secret** اضافه کنید:
   * `BOT_TOKEN`: توکن ربات تلگرام
   * `ADMIN_ID`: آیدی عددی تلگرام شما
   * `ENABLE_DOH`: مقدار `true`
6. پروژه را بیلد کرده و ارسال کنید:
   ```powershell
   # ۱. بیلد فایل اجرایی fatJar
   ./gradlew fatJar

   # ۲. اضافه کردن فایل ساخته شده به گیت و ارسال به هاگینگ‌فیس
   git add app.py packages.txt requirements.txt build/libs/telestream-all.jar -f
   git commit -m "Deploy via Gradio Free Tier"
   git remote add hf https://huggingface.co/spaces/<نام-کاربری>/<نام-اسپیس>
   git push hf main --force
   ```
7. هاگینگ‌فیس به صورت رایگان جاوا ۲۱ را نصب کرده و ربات شما با **۱۶ گیگابایت رم** شروع به کار می‌کند!

---

## 🚀 روش دوم: هاست ابری Render.com (داکر کاملاً رایگان بدون کارت بانکی)

سرویس [Render.com](https://render.com) امکان اجرای کانتینر Docker را به صورت کاملاً رایگان و **بدون نیاز به کارت بانکی** از روی گیت‌هاب فراهم می‌کند.

### مراحل:
1. در [render.com](https://render.com) یک حساب کاربری بسازید (با اکانت GitHub وارد شوید).
2. سورس TeleStream را روی یک مخزن خصوصی یا عمومی در اکانت گیت‌هاب خود Push کنید.
3. در داشبورد Render روی **New +** و سپس **Web Service** کلیک کنید.
4. مخزن گیت‌هاب `telestream` را انتخاب نمایید.
5. فیلدها را تنظیم کنید:
   * **Language / Environment**: گزینه **Docker** را انتخاب کنید (فایل `Dockerfile` آماده داخل سورس است).
   * **Region**: ترجیحاً Frankfurt (آلمان) یا Oregon.
   * **Instance Type**: حتماً روی گزینه **Free (0.1 CPU, 512 MB RAM)** کلیک کنید.
6. در بخش **Environment Variables**:
   * `BOT_TOKEN`: توکن تلگرام شما
   * `ADMIN_ID`: آیدی عددی شما
   * `PORT`: `7860`
7. روی **Deploy Web Service** کلیک کنید.

---

## ⚡ روش سوم: سرویس Koyeb (سریع و رایگان)

سرویس [koyeb.com](https://koyeb.com) یکی از بهترین ارائه‌دهندگان کانتینر رایگان بدون کارت بانکی است:
1. در سایت Koyeb ثبت‌نام کنید.
2. روی **Create Service** و سپس **GitHub** کلیک کنید.
3. ریپازیتوری خود را انتخاب کرده و حالت **Dockerfile** را بزنید.
4. فیلد `PORT` را روی `7860` و Instance Type را روی **Free (Nano)** قرار دهید.
5. توکن را در بخش Environment Variables اضافه و Deploy را بزنید.

---

## ⏰ روشن نگه داشتن ربات به صورت ۲۴ ساعته (جلوگیری از Sleep)

هاست‌های رایگان (هم هاگینگ‌فیس و هم Render) در صورتی که بعد از چند ساعت درخواستی دریافت نکنند به خواب می‌روند. برای جلوگیری از این موضوع:
1. آدرس عمومی سرویس خود را بردارید (مثلاً `https://your-bot.onrender.com/health` یا `https://your-space.hf.space/health`).
2. در سایت رایگان [cron-job.org](https://cron-job.org) یک کران‌جاب تنظیم کنید که **هر ۱۰ دقیقه یک‌بار** به این آدرس یک ریکوئست بفرستد.
3. با این کار، ربات شما **همیشه بیدار و ۲۴ ساعته** خواهد بود!
